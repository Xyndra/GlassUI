{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for the main window",
  "windows": ["settings"],
  "permissions": [
    "core:default"
  ]
}
